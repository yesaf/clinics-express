export type Clinic = {

}

export type Selectors = {
    city?: string,
    suburb?: string,
    clinicName?: string,
    zip?: string,
    state?: string,
}

export type Location = {
    lat: number,
    lng: number,
}
